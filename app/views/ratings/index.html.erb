<h1>Ratings</h1>

<div id="ratings">
  <% @ratings.each do |rating| %>
    <div class="rating">
      <%= simple_format Rating.content %>
    </div>
  <% end %>
</div>

<p><%= link_to "New Ratings", [:new, @ratable, :rating]%></p>
