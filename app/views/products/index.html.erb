<h1>Food</h1>

<h5> Artisan: <%= @user.email %> </h5>
  <% @products.each do |product| %>
    <%= link_to product.name, product_path(product) %><br/>
    <%= product.photo %><br/>
    <%= product.description %><br/>
    <%= product.price %><br/>
    <%= link_to "Delete information", event_path(event), data: { confirm: "Are you sure?" }, method: :delete %>
  <br/>

  <% end %>
